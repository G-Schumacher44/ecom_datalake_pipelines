name: "ecom_dbt_bigquery"
version: "0.1.0"
config-version: 2

profile: "ecom_dbt_bigquery"

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  ecom_dbt_bigquery:
    enriched_silver:
      +materialized: table
    gold_marts:
      +materialized: table
      +tags: ["gold"]
